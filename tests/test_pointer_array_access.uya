// 测试程序：验证指针类型数组访问

use std.testing.test_suite_begin;
use std.testing.test_suite_end;
use std.testing.run_test;

fn test_pointer_array_access() !void {
    var buffer: [byte: 100] = [];
    const buffer_ptr: &byte = &buffer[0] as &byte;
    const temp: byte = buffer_ptr[0];
    // 编译通过即测试通过
}

fn main() i32 {
    test_suite_begin("Pointer Array Access Tests");
    run_test("pointer array access", test_pointer_array_access);
    return test_suite_end();
}
