// 调试逻辑与运算

use std.testing.check_eq_i32;
use std.testing.check_eq_bool;
use std.testing.check_true;

test "test_logical_and" {
    const a: i32 = 10;
    const b: i32 = 20;
    check_eq_i32(a == b, false);
    check_eq_i32(a != b, true);
}
