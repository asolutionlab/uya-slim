// main() !i32 中 try 传播错误时，bridge 将 error_id 作为退出码

use std.testing;

error X;

fn f() !i32 {
    return error.X;
}

@test
fn test_return_error() void {
    const v: i32 = f() catch { 0; };
    testing.assert_eq(v, 0);
}
