// 测试 try 语法

use std.testing.check_eq_i32;
use std.testing.check_eq_bool;
use std.testing.check_true;

fn foo() !i32 { return 0; }

test "test_try_only" {
    const x: i32 = try foo();
    check_eq_i32(x, 0);
}
