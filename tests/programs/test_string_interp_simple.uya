// 测试简单字符串插值

use std.testing;

test "test_string_interp" {
    const x: i32 = 42;
    const msg: [i8: 32] = "x=${x}\n";
    // 验证编译正确，字符串插值在编译时展开
    testing.assert_eq(msg[0] == 120, true);  // 'x'
}
