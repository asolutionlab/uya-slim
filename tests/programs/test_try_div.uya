// 测试 try 除法

use std.testing;

error DivisionByZero;

fn divide(a: i32, b: i32) !i32 {
    return a / b;
}

@test
fn test_try_divide() void {
    const x: i32 = try divide(10, 2);
    testing.assert_eq(x, 5);
}
