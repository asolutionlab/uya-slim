// 最简单的 FFI 指针转换测试

use std.testing;

struct Node {
    id: i32,
}

@test
fn test_ptr_conversion() void {
    var node: Node = Node{ id: 1 };
    var node_ptr: &Node = &node;
    var ffi_ptr: *Node = node_ptr as *Node;
    testing.assert_eq(ffi_ptr == null, false);
}
