// 一元表达式测试
// 测试一元运算符（!, -）

use std.testing;

@test
fn test_unary_minus() void {
    const x: i32 = 10;
    testing.assert_eq(-x, -10);
}

@test
fn test_logical_not() void {
    testing.assert_eq(!true, false);
    testing.assert_eq(!false, true);
}
