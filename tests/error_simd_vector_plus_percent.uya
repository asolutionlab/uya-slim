// SIMD 第一阶段不支持向量包装加（运算符 +%）
type Vec4i32 = @vector(i32, 4);

export fn main() i32 {
    const a: Vec4i32 = @vector.splat(100);
    const b: Vec4i32 = @vector.splat(50);
    const c: Vec4i32 = a +% b;
    return 0;
}
