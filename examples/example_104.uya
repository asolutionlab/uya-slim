    // 源代码：使用 try 关键字进行溢出检查
    fn safe_add(a: i32, b: i32) !i32 {
        const result: i32 = try a + b;
        return result;
    }
     
     // 编译期展开为（伪代码）
     // if a > 0 && b > 0 && a > max - b {
     //     return error.Overflow;
     // }
     // if a < 0 && b < 0 && a < min - b {
     //     return error.Overflow;
     // }
     // return a + b;
     