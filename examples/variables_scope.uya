// 变量作用域示例

fn example() void {
    const x: i32 = 10;        // 不可变变量
    var i: i32 = 0;           // 可变变量，可重新赋值
    const arr: [f32: 4] = [1.0, 2.0, 3.0, 4.0];
    i = i + 1;                // 只有 var 变量可赋值
}
