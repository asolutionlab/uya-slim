// Portable microapp example: relocated global pointer -> rodata

use std.runtime.entry;
use std.microapp.io.write_stdout_bytes;

var g_msg: &const byte = "reloc ok\n";

export fn main() i32 {
    _ = write_stdout_bytes(g_msg, 9) catch {
        return 1;
    };
    return 0;
}
