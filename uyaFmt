#!/usr/bin/env bash
set -euo pipefail

# 独立格式化工具入口（包装编译器子命令）
# 用法:
#   ./uyaFmt path/to/file.uya
#   ./uyaFmt -w path/to/file.uya

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${ROOT_DIR}/bin/uya" fmt "$@"

