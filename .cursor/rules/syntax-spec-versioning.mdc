---
description: 全局要求：修改语法规范文档时同步升级规范版本
alwaysApply: true
---

# 语法规范文档版本同步

- 本规则全局总是生效；当任务涉及语法规范文档时，必须主动检查版本是否需要同步升级。
- 只要修改 `docs/uya.md`、`docs/grammar_formal.md`、`docs/grammar_quick.md` 或 `docs/builtin_functions.md` 的语法、语义、BNF、内建说明，就必须同步升级这组语法规范文档的版本号。
- 同步更新文档头部版本、版本同步说明、最后更新日期，以及文中引用到的规范版本，避免只改其中一份。
- 如果本次改动仅是错字、排版或不影响规范含义的描述修饰，可以不升级版本；若无法明确判断，默认按“需要升级版本”处理。
- 完成前检查这四份文档不存在新旧版本号混用。

示例：若在 `docs/grammar_formal.md` 新增一条正式语法，同时 `docs/uya.md` 与 `docs/grammar_quick.md` 也同步更新了对应说明，则应将它们的规范版本一致升级到下一版，而不是只改正文内容。
